#include "main.h"
/**
 * mul - this method multiplies two integers
 * @a: this is the first integer to be multiplied
 * @b: this is the second integer to be multiplied
 * Return: this method returns the multiplication Result.
 */
int mul(int a, int b)
{
	int multiply = a * b;

	return (multiply);
}
