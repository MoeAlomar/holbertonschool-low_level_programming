#include "main.h"
/**
 *print_last_digit - this method prints the last digit of a num
 *@n: is the integer provided from the user
 *
 *Returns: it returns the last digit immediately.
 */
int print_last_digit(int n)
{
	return (n % 10);
}
