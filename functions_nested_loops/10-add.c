#include "main.h"
/**
 * add - this function recieves two integers together and retuns them
 * @a: first integer to be summed
 * @b: second integer to be summed
 *Return: it returns integer of summing to integers
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;

	return (sum);
}
