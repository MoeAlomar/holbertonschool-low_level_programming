#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int count = 48;

	for (count = 48; count <= 57; count++)
{
	putchar(count);
}
	putchar('\n');
	return (0);
}
